server.port=8080
spring.application.name=idempotency-gateway

# Idempotency key TTL in minutes (keys expire after this duration)
idempotency.key.ttl-minutes=1440

# CORS allowed origin for React frontend
cors.allowed-origin=http://localhost:5173
